# Welcome to the Privacy-blur-test app

Needs to run on development build.

To build app from VSCode:

   cd android
   .\gradlew app:installDebug "-PreactNativeArchitectures=arm64-v8a"
   cd ..

To run on Android device
   
   npx expo start
   ```

